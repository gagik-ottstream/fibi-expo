import { StyleSheet } from 'react-native';

import { Styles } from './types';

export default StyleSheet.create<Styles>({
  header: {
    fontSize: 24,
    fontWeight: '600',
    marginBottom: 10
  },
  root: {
    padding: 10,
    width: 220
  }
});
