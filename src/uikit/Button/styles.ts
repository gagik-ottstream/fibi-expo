import { StyleSheet } from 'react-native';

import { Styles } from './types';

const styles = StyleSheet.create<Styles>({
  root: {
    backgroundColor: 'rgba(50, 36, 44, 0.5)',
    padding: 10
  }
});

export default styles;
